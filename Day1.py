#two sum
class Solution(object):
    def twoSum(self,nums,target):
        if len(nums) == 0
            return[]
        for index,item in enumerate(nums):
            for count in range(index+1,len(nums)):
                for item +nums[count] ==target
                    return[index,count]
                    
